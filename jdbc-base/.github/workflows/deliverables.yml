name: Create and upload jar file

env:
  PROJECT_NAME: jdbcbase

on:
  push:
    branches:
      - main
    tags:
      - v*.**
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set TAG-Env
        run: |
          if [[ ${{ github.ref  }} == 'refs/heads/main' ]]; then
            echo "NAME_TAG=${PROJECT_NAME}-${GITHUB_REF#refs/*/}" >> $GITHUB_ENV
          else
            echo "NAME_TAG=${PROJECT_NAME}-snapshot" >> $GITHUB_ENV
          fi
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
      - name: Show current application version
        run: ./gradlew cV
      - name: Execute Gradle build
        run: ./gradlew build
      - name: Store reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: |
            **/build/reports/
            **/build/test-results/
      - name: upload deliverables
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.NAME_TAG }}
          path: build/libs/
          if-no-files-found: error
