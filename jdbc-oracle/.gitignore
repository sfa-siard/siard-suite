# ignore all build directories
/build/
/build/**
# ignore javadoc folder
/doc/javadoc/**
# tmp contains temporary files
/tmp/**
# dist contains the binary build results
/dist/**
# sometimes junit4 produces temporary files that we do not want to check in
/junit*.*


target/

# intellij idea project files
.idea/
*.iml
out/

# asdf tools configuration
.tool-versions

# folder where the script build-oracle-image.sh will clone the oracle/docker-images repo into
docker-images

# gradle
.gradle/